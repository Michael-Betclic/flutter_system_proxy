## 0.2.0

* Added proxy cache invalidation when system proxy settings change (iOS).
* Improved PAC file handling for both inline JavaScript and remote PAC URLs (iOS).
* Minor stability improvements.

## 0.0.1

* Initial release: detect system HTTP proxy on Android and iOS.